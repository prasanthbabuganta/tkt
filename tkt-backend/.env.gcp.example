# ================================================================
# TKT Backend - GCP Environment Variables
# Copy this file and update with your actual values
# ================================================================

# GCP Project Configuration
GCP_PROJECT_ID=tkt-backend-prod
GCP_REGION=asia-south1

# Cloud SQL Configuration
CLOUD_SQL_INSTANCE=tkt-backend-prod:asia-south1:tkt-db-prod
DB_USERNAME=tkt-app-user
DB_PASSWORD=your-secure-database-password-here

# JWT Configuration (Generate strong random keys)
# Minimum 256 bits (32 characters) for security
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-256-bits-long-change-this-in-production
JWT_ACCESS_TOKEN_EXPIRY=2592000000
JWT_REFRESH_TOKEN_EXPIRY=5184000000

# Encryption Configuration
# MUST be exactly 32 bytes (32 characters) for AES-256
ENCRYPTION_SECRET_KEY=12345678901234567890123456789012

# Admin Seed Credentials (CHANGE THESE!)
ADMIN_SEED_MOBILE=9133733197
ADMIN_SEED_PIN=777777

# VPC Connector (if using Cloud SQL with private IP)
VPC_CONNECTOR=tkt-connector

# Cloud Run Service Account (optional)
SERVICE_ACCOUNT=tkt-backend-sa@tkt-backend-prod.iam.gserviceaccount.com

# Database Pool Configuration
DB_POOL_SIZE=20
DB_MIN_IDLE=10

# Logging Level
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=INFO
LOG_LEVEL_SECURITY=WARN

# ================================================================
# How to Generate Secure Keys
# ================================================================
#
# JWT Secret (256-bit):
#   openssl rand -base64 32
#
# Encryption Key (exactly 32 bytes):
#   openssl rand -hex 16
#   (This generates 32 hex characters = 16 bytes, then duplicate it)
#   OR use: head -c 32 /dev/urandom | base64 | cut -c1-32
#
# Strong Password:
#   openssl rand -base64 24
#
# ================================================================
# Security Best Practices
# ================================================================
#
# 1. NEVER commit this file with real values to git
# 2. Use GCP Secret Manager for production secrets
# 3. Rotate secrets regularly (every 90 days minimum)
# 4. Use different secrets for each environment
# 5. Limit access to secrets using IAM policies
# 6. Enable audit logging for secret access
#
# ================================================================
